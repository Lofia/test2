# https://jthomasmock.github.io/pkg-building/#153

library(devtools);library(usethis)
use_r('xxx.R')
command+option+shift+R
document()
check()

# terminal
source ~/.bashrc
git pull
git commit -a -m "update"
git push

# check
git status
git branch -v
git remote -v
